# Gradle daemon JVM criteria.
# QuPath's convention plugin cannot parse JDK 25+ version strings, so the daemon
# is pinned to Java 21 here instead of depending on the shell's JAVA_HOME.
# Gradle auto-detects a locally installed JDK 21; see README for install notes.
toolchainVersion=21
